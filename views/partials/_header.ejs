<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <!-- Logo -->
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <!-- Menu -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>

    <!-- Login -->
    <div class="navbar-brand">
      <!-- Logged in user + logout button -->
      <% if (user) { %>
        <div class ="login-container">
          <form class="form-inline" action="/logout" method="POST">
            <div style="margin-right: 25px;">Logged in as <strong><%= user.id %></strong></div>
            <button type="submit" class="btn btn-outline-light">Logout</button>
          </form>
        </div>
      <% } else { %>
        <!-- Not logged in, offers login or register option -->
        <div class ="login-container"> 
          <a href="/login"><button class="btn btn-outline-light" style="margin-right: 10px;">Login</button></a>
          <a href="/register"><button class="btn btn-light">Register</button></a>
        </div>
      <% } %>
    </div>
  </nav>
</header>